---
menu:
  elements:
  - section:
    - name: Heading
      hint: Insert different size heading with optional sub-heading and alignment options.
      component: elements_heading
      icon: heading
      enabled: true
    - name: Divider
      hint: Insert horizontal divider with optional icon and heading.
      component: elements_divider
      icon: ellipsis horizontal
      enabled: true
    - name: Rich Text
      hint: Add text with rich formatting, links, and lists.
      component: elements_richtext
      icon: file alternate
      enabled: true
    - name: Embed
      hint: "Insert rich media (i.e. tweet) from a link."
      component: elements_embed
      icon: plug
      enabled: true
    - name: Image
      hint: Insert an image, with optional alignment and formatting options.
      component: elements_image
      icon: image
      enabled: true
    - name: Full-Screen Embed
      hint: "Insert full-screen link embedded into content."
      component: elements_oversizedembed
      icon: plug
      enabled: true
    - name: Full-Screen Image
      hint: Insert full screen image with optional title and caption.
      component: elements_oversizedimage
      icon: images
      enabled: true
    - name: Full-Screen Video
      hint: "Insert full-screen video with optional title."
      component: elements_video
      icon: video
      enabled: true
    - name: Quote
      hint: Add a quote with an authors name.
      component: elements_blockquote
      icon: quote left
      enabled: true
    - name: Full-Screen Quote
      hint: "Insert full-screen quote with a background image."
      component: elements_oversizedquote
      icon: quote left
      enabled: true
    - name: Reference
      hint: APA style citation, with name, author, reference and link.
      component: elements_reference
      icon: graduation cap
      enabled: true
    - name: Solid Header
      hint: Insert header with solid background, title, and optional summary.
      component: elements_solidheader
      icon: newspaper
      enabled: true
    - name: Image Header
      hint: Insert header with background image, title, and optional summary.
      component: elements_imageheader
      icon: camera retro
      enabled: true
    - name: Embeded Header
      hint: Insert header with solid background, title, and rich media (i.e. tweet) from a link.
      component: elements_embededheader
      icon: hdd
      enabled: true
    - name: Video Header
      hint: Insert video header with background image and an optional title.
      component: elements_videoheader
      icon: play circle outline
      enabled: true
    - name: Color Block
      hint: " Insert color block with optional title and description."
      component: elements_colorblock
      icon: tint
      enabled: true
