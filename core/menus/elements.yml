---
menu:
  elements:
  - section:
    - name: Heading
      hint: Add different size headings with optional sub-headings.
      component: elements_heading
      icon: heading
      enabled: true
    - name: Divider
      hint: Insert a line divider with optional heading and an icon.
      component: elements_divider
      icon: ellipsis horizontal
      enabled: true
    - name: Rich Text
      hint: Add text with rich formatting, links, and lists.
      component: elements_richtext
      icon: file alternate
      enabled: true
    - name: Image
      hint: Insert an image, with optional alignment and formatting options.
      component: elements_image
      icon: image
      enabled: true
    - name: Image
      hint: An oversized image with optional title and caption.
      component: elements_oversizedimage
      icon: image
      enabled: true
    - name: Quote
      hint: Add a quote with an authors name.
      component: elements_blockquote
      icon: quote left
      enabled: true
    - name: Quote
      hint: " An oversized quote with an image."
      component: elements_oversizedquote
      icon: quote left
      enabled: true
    - name: Reference
      hint: APA style citation, with name, author, reference and link.
      component: elements_reference
      icon: graduation cap
      enabled: true
    - name: Solid Header
      hint: Insert page header with solid background, title, and optional summary.
      component: elements_solidheader
      icon: newspaper
      enabled: true
    - name: Image Header
      hint: " An image header with title and optional summary."
      component: elements_imageheader
      icon: camera retro
      enabled: true
    - name: Embed
      hint: "Insert rich media (i.e. tweet) from a link."
      component: elements_embed
      icon: plug
      enabled: true
    - name: Embeded Header
      hint: Insert page header with solid background, title, and rich media (i.e. tweet) from a link.
      component: elements_embededheader
      icon: hdd
      enabled: true
    - name: Video Header
      hint: " A header with an embedded video."
      component: elements_videoheader
      icon: play circle outline
      enabled: true
    - name: Embed
      hint: " An oversized link embedded into content."
      component: elements_oversizedembed
      icon: plug
      enabled: true
    - name: Color Block
      hint: " An oversized color block with optional title and description."
      component: elements_colorblock
      icon: tint
      enabled: true
    - name: Video
      hint: " An oversized block with video element and optional title."
      component: elements_video
      icon: video
      enabled: true
