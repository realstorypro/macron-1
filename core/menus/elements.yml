---
menu:
  elements:
  - section:
    - name: Heading
      hint: Different size headers with optional subheaders.
      component: elements_heading
      icon: heading
      enabled: true
    - name: Divider
      hint: A content divider with optional heading and icon.
      component: elements_divider
      icon: ellipsis horizontal
      enabled: true
    - name: Rich Text
      hint: Paragraphs with formatting, links, and lists.
      component: elements_richtext
      icon: file alternate
      enabled: true
    - name: Image
      hint: Image, with optional alignment and formatting options.
      component: elements_image
      icon: image
      enabled: true
    - name: Oversized Image
      hint: An oversized image with optional title and caption.
      component: elements_oversizedimage
      icon: image
      enabled: true
    - name: Quote
      hint: A quote with an authors name.
      component: elements_blockquote
      icon: quote left
      enabled: true
    - name: Oversized Quote
      hint: " An oversized quote with an image."
      component: elements_oversizedquote
      icon: quote left
      enabled: true
    - name: Reference
      hint: APA style citation, with name, author, reference and link.
      component: elements_reference
      icon: graduation cap
      enabled: true
    - name: Solid Header
      hint: A solid color header with title, and optional summary.
      component: elements_solidheader
      icon: newspaper
      enabled: true
    - name: Image Header
      hint: " An image header with title and optional summary."
      component: elements_imageheader
      icon: camera retro
      enabled: true
    - name: Embed
      hint: " An object that embeds a link into the website"
      component: elements_embed
      icon: plug
      enabled: true
    - name: Embeded Header
      hint: " A header with an embedded URL."
      component: elements_embededheader
      icon: hdd
      enabled: true
    - name: Video Header
      hint: " A header with an embedded video."
      component: elements_videoheader
      icon: play circle outline
      enabled: true
    - name: Oversized Embed
      hint: " An oversized link embedded into content."
      component: elements_oversizedembed
      icon: plug
      enabled: true
