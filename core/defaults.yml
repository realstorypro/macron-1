defaults:

  permissions:
    # default role assigned to the visitor
    visitor:
      role: guest

    # default role assigned to the new user
    new_user:
      role: member

  # the root route of the application
  root_route: 'page#home'
