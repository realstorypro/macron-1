= widget name: 'cover', id: "cover#{rand.to_s[2..4]}", vue: :on, data: { image: options[:image], landscape_image: options[:landscape_image], vertical_image: options[:vertical_image] }
  = image ui: :off, class: "#{'fixed' if overlay_background == 'fixed'}"
  = overlay
