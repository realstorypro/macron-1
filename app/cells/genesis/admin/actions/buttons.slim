- actions.each do |action|
  - if action[:permission]
    = button class: build_button_class(action[:class]), url: action[:url], data: action[:data]
      = icon action[:icon] if icons? && icon_position.eql?(:left)
      = action[:text]
      = icon action[:icon] if icons? && icon_position.eql?(:right)
